  // variável boolean
  let itsOn = true;
​
// se a variável tiver o valor "true" o comando do if será executado.
if (itsOn) {
  console.log("Executou comando!");
}
​
  let itsEmpty = false;
  ​
if (itsEmpty) {
  console.log("Está vazio!");
} 
// caso o if não seja válido e executado, o comando de else será executado.
else {
  console.log("Está lotado!");
}
  
  let number = 5;
​
if (number == 3) {
  console.log("o número é 3!");
} 
// caso o if não seja válido e executado, 
// o comando de else if será validado e executado.
else if (number == 5) {
  console.log("o número é 5!");
}
